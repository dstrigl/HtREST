[tox]
envlist = py35, py36, flake8
;skipsdist = True

[travis]
python =
    3.6: py36, flake8
    3.5: py35, flake8

[testenv]
deps = -rrequirements-dev.txt
commands =
    python --version
    ;pip install -U pip
    ;pip install -rrequirements-dev.txt
    pytest --verbose htrest tests

[testenv:flake8]
basepython = python
deps = -rrequirements-dev.txt
commands =
    python --version
    ;pip install -U pip
    ;pip install -rrequirements-dev.txt
    flake8 htrest tests setup.py

[testenv:cov-report]
basepython = python
deps = -rrequirements-dev.txt
commands =
    python --version
    ;pip install -U pip
    ;pip install -rrequirements-dev.txt
    pytest --verbose --cov=htrest --cov-report=term

[testenv:connected]
basepython = python
deps = -rrequirements-dev.txt
commands =
    python --version
    ;pip install -U pip
    ;pip install -rrequirements-dev.txt
    pytest --verbose --connected {posargs}
